{# Firm node, rebuilt each Eleventy run from site.json + nav.json + team.
   Dual type LegalService + Organization so Article author/publisher can
   point at the same @id. Filters live in eleventy.config.js. #}
<script type="application/ld+json">{{ site | jsonldOrg(nav, collections.team) | safe }}</script>
