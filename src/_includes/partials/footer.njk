<footer class="site-footer">
  <div class="wrap site-footer__cols">
    <div class="footer-col footer-col--firm">
      {% include "partials/logo.njk" %}
      <address>
        {{ site.name }}<br>
        {{ site.address.street }}<br>
        {{ site.address.postalCode }} {{ site.address.city }}
      </address>
      <a href="{{ site.phoneHref }}">{{ site.phone }}</a><br>
      <a href="mailto:{{ site.email }}">{{ site.email }}</a>
      <p class="footer-col__note">Adwokaci – Izba Adwokacka w Warszawie.
      Radcowie prawni – Okręgowa Izba Radców Prawnych.</p>
    </div>
    {# The category hubs, not the 22 services. Every service is already one click
       from the homepage, so listing them here bought no crawl reach and diluted
       22 site-wide anchors into noise. Rendered from nav.categories, so the
       footer can never fall behind the taxonomy again. #}
    <nav class="footer-col" aria-label="Oferta">
      <h2 class="footer-col__title">Oferta</h2>
      <ul>
        {% for cat in nav.categories %}
        <li><a href="{{ cat.url }}">{{ cat.label }}</a></li>
        {% endfor %}
        <li><a href="/oferta/">Wszystkie usługi</a></li>
      </ul>
    </nav>
    {% for col in nav.footer %}
    <nav class="footer-col" aria-label="{{ col.title }}">
      <h2 class="footer-col__title">{{ col.title }}</h2>
      <ul>
        {% for item in col.items %}
        <li><a href="{{ item.url }}">{{ item.label }}</a></li>
        {% endfor %}
      </ul>
    </nav>
    {% endfor %}
  </div>
  <div class="site-footer__bottombar">
  <div class="wrap site-footer__bottom">
    <p class="site-footer__registry">© {% year %} {{ site.name }} ·
      KRS {{ site.registry.krs }} · NIP {{ site.registry.nip }} ·
      REGON {{ site.registry.regon }}</p>
    <ul class="site-footer__legal">
      {% for item in nav.bottomBar %}
      <li><a href="{{ item.url }}">{{ item.label }}</a></li>
      {% endfor %}
    </ul>
  </div>
  </div>
</footer>
